<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .error {
            color: red;
            margin-bottom: 10px;
        }
        form {
            display: flex;
            flex-direction: column;
        }
        label {
            margin-top: 10px;
        }
        textarea, input[type="url"] {
            margin-top: 5px;
            padding: 10px;
            width: 100%;
            box-sizing: border-box;
        }
        button {
            margin-top: 20px;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Edit Resource</h1>
    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } %>
    <form action="/api/v1/admin/resource/<%= resource._id %>/edit?_method=PUT" method="POST">
        <input type="hidden" name="_method" value="PUT">
        <label for="resourceContext">Resource Context:</label>
        <textarea name="resourceContext" required><%= resource.resourceContext %></textarea>
        <label for="resourceUrl">Resource URL:</label>
        <input type="url" name="resourceUrl" value="<%= resource.resourceUrl %>" required>
        <button type="submit">Update Resource</button>
    </form>
</body>
</html>